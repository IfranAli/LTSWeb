import {createAction} from "@ngrx/store";

export const createProject = createAction('Create project action');
export const updateProject = createAction('Update project action');
export const deleteProject = createAction('Delete project action');
export const loadProjects = createAction('Load project action');
